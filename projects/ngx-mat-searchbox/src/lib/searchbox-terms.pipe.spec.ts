import { NgxMatSearchboxTermsPipe } from './searchbox-terms.pipe';

describe('SearchboxTermsPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxMatSearchboxTermsPipe();
    expect(pipe).toBeTruthy();
  });
});
